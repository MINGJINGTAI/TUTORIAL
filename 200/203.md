# 203.镜像分层与分层规范

**一、前言**

在上一篇教程《202.镜像差分》中，我们可以对镜像文件制作父子差分的格式，形成一套较为完整的的镜像体系。在实际制作过程中，多个文件的存在，第一个难点就会立刻出现，“怎么给这些有关联关系的文件命名”？

![图片](https://mmbiz.qpic.cn/mmbiz_png/sgxgSdyNAarwgF3AWSibLVR0bn7PvmI1icgJSd3TT8FftibFOGvricXa51fbv3Rqj76UxvBrpFLOG5KlCKZbjwIlcQ/640?wx_fmt=png&from=appmsg&tp=nowebp&wxfrom=5&wx_lazy=1&wx_co=1)

本篇教程就是来解决这个问题，提供了一些参考建议。

**二、格式约定**

云固件通常使用VHDx作为标准的镜像格式，仅仅在Windows 7系统上使用VHD格式。

云固件制作VHD(x)的方法和微软提供的VHD(x)镜像格式制作过程有很大的不同。具体差异主要表现在微软要求的本地启动VHD镜像使用位于VHD(x)文件之外的引导管理器（Boot Loader），而云固件使用VHD(x)镜像文件内ESP分区内的引导管理器。因此，云固件使用的VHD(x)镜像文件是可以作为虚拟机的磁盘启动的，但微软要求的本地启动VHD(x)镜像是无法启动，原因是缺少ESP分区。

VHD(x)镜像格式支持Fixed、Dynamic、Differencing三种模式，其中Differencing模式（差分模式）可以生成父子镜像，提供了快照、回滚等多种高级功能，云固件充分利用了这些能力来方便用户。

云固件提供的标准镜像文件，默认使用VHDx动态格式，容量为256G，区块大小为默认值；分区使用GPT格式。

云固件的镜像文件名通常使用“-”来区分各段名称，格式为“系统名称-版本-层级-机型.vhdx”。如前言中的Windows镜像，名称为“Windows10-22H2-Pro-L3-TF.vhdx”，Ubuntu Kylin的名称如“Ubuntu-Kylin-23.10-Pro-L3.vhdx”。



**三、分层模式**

在格式约定中，读者可以看到“层级”这个概念。云固件设计目的是为了简化系统部署，其中分层设计是最重要的一个概念。

什么是分层？简单举个例子，我们要在一台计算机上部署办公环境，常用环境只有邮件和文档，使用微软Office 2019专业版。这时候，我们需要做的事情包括安装Windows操作系统、安装驱动程序、更新操作系统补丁、安装Office办公套件、设置账号信息、重启确认工作正常。这中间的全部步骤都是分段进行的，也极有可能中间出现差错，导致重新开始。为了避免重复，最好的方式就是分段备份，出错就恢复备份。

这个分段进行的方式其实就是最朴素的分层方式。

由于传统部署方式，无法简单备份，所以需要依赖第三方工具。但使用云固件模式后，只需备份镜像文件即可实现完整备份了。所以，这些层级的备份文件就是最简单的分层模式备份了。这是第一种分层模式，全量分层。

第二种分层模式，是通过差分获得。在上面的安装过程中，确认一层没问题后，选择差分，剩下的操作都是在子镜像内完成，出现错误，删除子镜像，重新差分再来。这个过程得到的镜像，就是增量分层。

总结一下，分层模式分成全量分层和增量分层两种，对比备份就是全量备份和增量备份。

上面前言中的Windows镜像文件夹截图属于增量分层模式。

**四、分层规范**

定义清楚分层模式后，都会遇到在哪个阶段分层的问题。为了让制作的镜像文件能够得到最大化的利用，云固件提出了如下分层规范：

1. 分层按照L0-L9来定义；
2. 高层级镜像原则上要求包含低层级全部镜像内容（全量分层）；
3. 除L0级别外，每个层级均可以生成差分级别（增量分层）；
4. 同层级的差分镜像使用层级加两位数的顺序号来区分，如301，305，501，511等（增量分层）；

云固件的分层规范是全量分层与增量分层的混合体，读者注意区分。

**五、分层约定**

根据分层规范要求，约定各层级的具体要求。

### **L0层**

L0层为待安装操作系统层，仅对Windows系列有效。使用GPT格式进行磁盘分区；可通过dism命令将wim文件展开到系统分区，使用bcdboot添加引导文件（该方式已经在后期的Windows 10/11上失效）；亦可通过Windows安装程序在虚拟机内进行部署至待重启为止。

### **L1层**

本层为基本安装层。Windows系统或者Linux执行基本安装过程，安装期间断开网络，防止自动更新；用户名me，无密码或者有密码统一为“I@mtheNo.1”，不包含引号部分；

### **L2层**

L2层为完善驱动层。本层在L1的基础上，安装完善宿主机需要的全部驱动程序，包括自动更新驱动的服务程序，如Thinkpad的System Updater、NUC上的Intel® Driver & Support Assistant (Intel® DSA)等。

对于DIY主机，由于独立显卡的差异，本层会分化出一个L2.5层，满足针对N卡、A卡、I卡（Intel Arc系列独立显卡）一次性部署需求。

针对Linux系统，也需更新包括集成显卡、独立显卡、网卡等驱动程序。

### **L3层**

L3层为系统更新层。本层执行系统非跨版本更新，更新时限为制作时。如Windows 10 1809，不可跨版本升级到1903版本。

### **L4层**

L4层为系统激活层。用户根据自身需要使用企业内部KMS服务器激活Windows系统。默认激活服务器为KMS.local。用户也可以在本层使用sysprep更新用户设置信息。

### **L5-L9层**

L5到L9层为用户自定义层。用户可以在本层安装用户所需的应用程序或者数据文件，比如Microsoft Office、电子邮件客户端、开发工具等。用户也可以在这些层级上定义不同目的镜像分层。



**六、分层例外及特殊情况**

本规范要求的分层约定能满足常见系统的使用要求，但并非所有系统均能满足这些约定，因此本约定为建议规范。一些常见的例外情况如下：

1. 品牌PC厂商通常会提供OEM激活的Windows Home版操作系统。Windows Home版的系统镜像部署后，会立即满足激活条件，用户可在L2层上即可看到Windows系统已经被激活。
2. Windows系统在L4层的激活过程，由于很多企业并未购买KMS服务，但还是存在L5-L9层的应用环境需求，所以提供这类镜像时，必须要在镜像的说明文件中明确系统未激活，或者使用180天试用激活。
3. Linux系统不存在Windows系统的释放过程，但很多安装ISO镜像文件提供LiveCD的体验环境，因此，Linux系统可封装出L0层级的可启动ISO镜像环境。本层级无法被后续更高层级继承。

**七、总结**

云固件的镜像分层与分层规范是指导性设计与要求，核心目的是为了减少云固件镜像的生产、流通、消费三个环节中的重复劳动。

当然，特定场合、特定群体不一定必须遵循此指导性规范，灵活的使用云固件与云固件镜像才是最佳方案。